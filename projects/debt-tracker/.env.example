# debt-tracker needs one key. Data lives in a local SQLite file (data/debt-tracker.db)
# and receipts in a local folder (data/receipts/), so there are no database credentials.
GEMINI_API_KEY=your_gemini_api_key_here
